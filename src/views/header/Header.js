import React, { PropTypes } from 'react'
import Menu from './Menu'

const Header = ({}) => (
    <div className="App-header">
        <h2>Ciuchy Kraków</h2>
        <Menu />
    </div>
)

export default Header
